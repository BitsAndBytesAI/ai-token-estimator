---
"ai-token-estimator": minor
---

Add a pure TypeScript SentencePiece tokenizer (Unigram + SentencePiece-style BPE + HF `tokenizer.json` JSON-BPE), removing the `sentencepiece`/`sentencepiece-js` dependency while keeping the public API compatible.
