---
"ai-token-estimator": minor
---

Add Phase 2 OpenAI tokenizer enhancements: `countTokens()` (exact for OpenAI, heuristic otherwise) and optional tokenizer modes for `estimate()` (`heuristic`/`openai_exact`/`auto`).
